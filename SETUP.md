# 予定よみとり — Google連携の初期設定ガイド

アプリがあなたのGoogleカレンダーに直接書き込めるようにするための設定です。
**最初の1回だけ**。所要30〜45分。パソコンでやるのがおすすめです。

> **用語**
> - **Google Cloud**: Googleが提供する開発者向けの管理画面。ここで「このアプリにカレンダーを触らせていい」という許可証を作ります。
> - **クライアントID**: その許可証の番号。`1234-abcd.apps.googleusercontent.com` のような文字列。パスワードではないので、誰かに見られても直ちに悪用はされません（後述の「生成元」で守られています）。
> - **スコープ**: アプリに渡す権限の範囲。今回は「予定の作成・編集だけ」で、メールやドライブには一切触れません。

---

## STEP 1: プロジェクトを作る

1. https://console.cloud.google.com/ を開く（自分のGoogleアカウントでログイン）
2. 初回は国の選択と利用規約への同意画面が出る → 日本 / 同意して続行
3. 画面上部の**プロジェクト選択メニュー**（「プロジェクトを選択」と書いてある所）をクリック
4. 右上の「**新しいプロジェクト**」→ プロジェクト名に `schedule-app` と入力 → 「作成」
5. 作成後、上部のメニューが `schedule-app` になっていることを確認（なっていなければ選び直す）

⚠️ お金はかかりません。カレンダーAPIは無料枠だけで使えます。クレジットカード登録も不要です。

---

## STEP 2: カレンダーAPIを使えるようにする

1. 画面上部の検索窓に `Google Calendar API` と入力して検索
2. 「Google Calendar API」を選ぶ
3. 青い「**有効にする**」ボタンを押す（すでに有効なら「管理」と表示される）

---

## STEP 3: 同意画面を作る

「このアプリにカレンダーを触らせますか？」とあなたに聞くための画面の設定です。

1. 左メニューから「**APIとサービス**」→「**OAuth同意画面**」
   （新しいUIでは「**Google Auth Platform**」→「**ブランディング**」）
2. User Type は「**外部**（External）」を選んで「作成」
3. 入力するのは3つだけ:
   - アプリ名: `予定よみとり`
   - ユーザーサポートメール: 自分のGmail
   - デベロッパーの連絡先情報: 自分のGmail
4. 「保存して次へ」を押していく。**スコープの画面は何も追加せず**そのまま次へでOK
5. 「**テストユーザー**」の画面（新UIでは「対象」→「テストユーザー」）で
   「**+ ADD USERS**」→ **自分のGmailアドレスを追加** → 保存

⚠️ **STEP 3-5 は絶対に忘れないでください。** ここに自分を追加しないと、連携しようとしても「アクセスをブロックしました」と出て先に進めません。

---

## STEP 4: クライアントIDを作る

1. 左メニュー「**認証情報**」（新UIでは「**クライアント**」）
2. 上部の「**+ 認証情報を作成**」→「**OAuth クライアント ID**」
3. アプリケーションの種類: 「**ウェブ アプリケーション**」
4. 名前: `schedule-web`（何でもOK）
5. 「**承認済みの JavaScript 生成元**」に「+ URIを追加」で次の2つを追加:
   ```
   https://pongit-app.github.io
   ```
   ```
   http://localhost:8902
   ```
6. 「承認済みのリダイレクトURI」は**空のままでOK**
7. 「作成」を押すと **クライアントID** が表示される → コピー

> この「生成元」に登録したアドレスからしか使えない仕組みなので、
> クライアントIDが他人に見られても、その人のサイトからは使えません。

---

## STEP 5: アプリに貼り付ける

1. アプリ（schedule.html / 公開後は GitHub Pages のURL）を開く
2. 一番上の「クライアントID」欄にコピーしたIDを貼り付ける
3. 「**Googleと連携する**」を押す
4. Googleのログイン画面 → 「**このアプリは Google で確認されていません**」と出たら
   「**詳細**」→「**予定よみとり（安全ではないページ）に移動**」
   （自分で作った自分専用アプリなので問題ありません）
5. 「カレンダーの予定の表示と編集」に「**続行**」
6. 画面右上が「**連携ずみ ✓**」になれば成功

クライアントIDはその端末に保存されるので、次からは貼り直し不要です。

---

## よくあるつまずき

| 症状 | 原因と対処 |
|---|---|
| `アクセスをブロックしました: このアプリは...` | STEP 3-5 のテストユーザーに自分を追加していない |
| `redirect_uri_mismatch` / `origin mismatch` | STEP 4-5 の JavaScript 生成元が違う。**末尾に `/` を付けない**、`https` と `http` を間違えない |
| 連携ボタンを押しても何も起きない | ポップアップがブロックされている。ブラウザのアドレスバー右のブロック表示から許可する |
| しばらく使うと登録に失敗する | アクセス権の有効期限（約1時間）切れ。「Googleと連携する」をもう一度押すだけ |
| 「テストモードは7日で切れる」と聞いた | それはリフレッシュトークンの話。このアプリは毎回その場で許可を取る方式なので影響ありません |

---

## 使い方（設定が終わったら）

1. 📷 写真・スクショを選ぶ（複数枚まとめてOK）
2. 🔍 読み取って予定を抜き出す（初回だけ日本語データのDLで20秒ほど）
3. 出てきた予定カードで、タイトル・日付・時刻を**目で確認して修正**
4. 登録しないものはチェックを外す
5. 下の「カレンダーに N件 登録する」を押す

読み取りがうまくいかない時は「読み取った文字を見る／直して再解析」を開いて、
文字を手で直してから「このテキストから予定を抜き出す」を押してください。
LINEなどからコピーした文章を直接貼り付けて使うこともできます。
